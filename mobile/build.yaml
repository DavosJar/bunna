targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
          # Sin field_rename global a propósito: cada DTO declara su propio
          # FieldRename (snake para identidad; explícito por campo en fincas).
          # Ver mobile/docs/ARQUITECTURA.md §5.
